---
trigger: always_on
---

# Правила работы с репозиторием Heat Up

## 🎯 О проекте

Heat Up — сервис автоматического прогрева Telegram-аккаунтов через LLM-генерируемое поведение.

**Production:**
- Сервис: `python main.py` (FastAPI на порту 8080)
- База данных: `data/sessions.db` (SQLite)
- Логи: `logs/heat_up.log`

## 📂 Структура файлов

```
heat_up/
├── main.py, scheduler.py, executor.py  # Основной код (НЕ ТРОГАТЬ без причины)
├── data/                               # Данные и конфигурации
├── docs/
│   ├── guides/                         # Актуальные руководства
│   └── archive/                        # Архив (устаревшее)
├── scripts/                            # Вспомогательные скрипты
├── tests/                              # Тесты
└── .agent/workflows/                   # Правила для агентов
```

## ❌ Запрещено

1. **Создавать `.md` файлы в корне репозитория**
   - Все отчеты → `docs/archive/reports/`
   - Все руководства → `docs/guides/`

2. **Файлы с названиями:**
   - `*_REPORT.md`, `*_SUMMARY.md`, `FINAL_*.md`
   - Дубликаты существующей документации

3. **Изменять без явного запроса:**
   - `.env` файл
   - `data/sessions.db`
   - Основной код production (`main.py`, `scheduler.py`)

## ✅ Разрешено

1. Создавать скрипты в `scripts/`
2. Создавать тесты в `tests/`
3. Обновлять README.md при добавлении функций
. Создавать workflows в `.agent/workflows/`

## 🔧 Перед началом работы

```bash
# Проверить что сервис работает
curl http://localhost:8080/health

# Проверить статус scheduler
curl http://localhost:8080/scheduler/status

# Посмотреть активные процессы
ps aux | grep "python.*main.py"
```

## 📊 Полезные команды

```bash
# Статистика аккаунтов
curl http://localhost:8080/statistics | jq

# Список аккаунтов
curl http://localhost:8080/accounts | jq

# Логи в реальном времени
tail -f logs/heat_up.log
```

## ⚠️ Production Safety

- Сервис работает 24/7, прогревает ~60 сессий
- Перед изменениями в коде — проверять импорты
- После изменений — проверять `curl /health`
- НЕ перезапускать сервис без необходимости