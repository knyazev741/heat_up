# Улучшения чтения каналов как человек

## Проблема

### Старая логика (продакшн `executor.py`):
```python
# ❌ НЕПРАВИЛЬНО:
# 1. Читает только последние 10 сообщений (не с первого непрочитанного)
history_result = await self.telegram_client.get_chat_history(
    session_id, resolved_peer, limit=10
)

# 2. Фиксированное время независимо от количества текста
await asyncio.sleep(duration)  # duration = 5 сек всегда
```

**Что не так:**
- Не начинает с первого непрочитанного сообщения
- Не учитывает длину текста
- Нет вариативности в поведении

## Решение

### Новая логика (тестовый скрипт `tests/test_read_channels.py`):

#### 1. **Читает с первого непрочитанного**

```python
# ✅ ПРАВИЛЬНО: начинаем с read_inbox_max_id + 1
first_unread_id = read_inbox_max_id + 1

query = make_get_history_query(
    peer=peer_info['input_peer'],
    offset_id=first_unread_id,  # Начинаем с первого непрочитанного
    add_offset=0,
    limit=messages_to_read
)
```

**Результат:** Скролл встает на самое старое непрочитанное, как в клиенте

#### 2. **Динамическое время чтения**

```python
# ✅ ПРАВИЛЬНО: время зависит от длины текста
for msg in messages_sorted:
    text_length = len(msg.get('message', ''))
    
    # Скорость чтения: 3-6 символов в секунду (200-300 символов/минуту)
    reading_speed = random.uniform(3, 6)
    reading_time = text_length / reading_speed
    
    # Добавляем базовое время и время на осмысление
    base_time = 1.0  # Минимум 1 сек на любое сообщение
    thinking_time = random.uniform(0.5, 2.0)
    
    msg_read_time = base_time + reading_time + thinking_time
    msg_read_time = min(msg_read_time, 30.0)  # Максимум 30 сек
    
    await asyncio.sleep(msg_read_time)
```

**Примеры:**
- 50 символов: 1 + 50/4.5 + 1.2 = ~13 сек
- 200 символов: 1 + 200/4.5 + 1.5 = ~47 сек → ограничено до 30 сек
- 500 символов: 1 + 500/4.5 + 1.8 = ~113 сек → ограничено до 30 сек

#### 3. **Пролистывание некоторых сообщений**

```python
# ✅ ПРАВИЛЬНО: 15% сообщений пролистываются быстро (если 3+ непрочитанных)
skip_probability = 0.15 if len(messages_sorted) >= 3 else 0

for msg in messages_sorted:
    if random.random() < skip_probability:
        # Быстрый скролл - не вникая в содержание
        msg_read_time = random.uniform(0.3, 0.8)
    else:
        # Реальное чтение (см. выше)
        ...
```

**Поведение:** Если в канале 10 непрочитанных, ~1-2 из них будут пролистаны быстро (0.3-0.8 сек)

#### 4. **Правильная маркировка как прочитанное**

```python
# ✅ ПРАВИЛЬНО: для каналов используем channels.ReadHistory
# telegram_tl_helpers.py
if 'Channel' in peer_type:
    channel = pylogram.raw.types.InputChannel(
        channel_id=peer.channel_id,
        access_hash=peer.access_hash
    )
    raw_method = pylogram.raw.functions.channels.ReadHistory(
        channel=channel,
        max_id=max_id
    )
else:
    # Для чатов/юзеров
    raw_method = pylogram.raw.functions.messages.ReadHistory(
        peer=peer,
        max_id=max_id
    )
```

## Результаты тестирования

### Тест на сессии 27067:

```
📖 Канал: @tass_agency
   Непрочитанных: 1
   Сообщение #346980 (180 симв.): 30.0с
   ✅ Помечено как прочитанное

📖 Канал: @meduzalive
   Непрочитанных: 50
   Диапазон: #133519 - #133568 (первое непрочитанное → последнее)
   Прочитано за 82.7 сек (среднее 1.7с/сообщение)
   Некоторые пролистаны быстро (0.3-0.8с)
   ✅ Все помечены как прочитанные
```

### Поведение как у человека:

✅ Скролл встает на первое непрочитанное  
✅ Время зависит от длины текста  
✅ Случайные вариации в скорости чтения  
✅ Иногда пролистывает не вникая  
✅ Правильно помечает как прочитанное  

## Интеграция в продакшн

### ✅ Исправлен `executor.py` - метод `_read_messages()`:

1. **Правильное чтение с первого непрочитанного:**
   ```python
   first_unread_id = read_inbox_max_id + 1
   query = make_get_history_query(
       peer=resolved_peer['input_peer'],
       offset_id=first_unread_id,
       add_offset=0,
       limit=max_messages_to_read
   )
   ```

2. **Динамическое время чтения:**
   ```python
   base_time = 1.0
   reading_speed = random.uniform(3, 6)
   reading_time = text_length / reading_speed if text_length > 0 else 0
   thinking_time = random.uniform(0.5, 2.0)
   msg_read_time = base_time + reading_time + thinking_time
   msg_read_time = min(msg_read_time, 30.0)
   ```

3. **Вероятность быстрого пролистывания:**
   ```python
   skip_probability = 0.15 if len(messages_sorted) >= 3 else 0
   if random.random() < skip_probability:
       msg_read_time = random.uniform(0.3, 0.8)
   ```

4. **Улучшенное логирование:**
   ```
   📨 Unread messages in channel: 15
      First unread: #123, Last: #137
   📥 Reading 15 unread messages starting from #123...
   📥 Got 15 unread messages
      Range: #123 - #137
     📬 Msg #123 (245 chars, 5.2s): Some message text...
   ✅ Read 15 messages in 78.4s (avg 5.2s/msg)
   ```

5. **Безопасность:**
   - Ограничение: максимум 50 сообщений за раз
   - Для уже прочитанных каналов - просматривает последние 3-5 для вида

### Что ещё нужно сделать

1. **Фиксировать `is_joined=1` в БД** - когда вступаем в канал
2. **Добавить в action plan** - LLM должна планировать чтение каналов (если ещё не добавлено)

## Файлы

- **Тестовый скрипт:** `tests/test_read_channels.py` ✅
- **Исправлено:** `telegram_tl_helpers.py` (функция `make_read_history_query`) ✅
- **Исправлено:** `executor.py` (метод `_read_messages`) ✅

